import { FaDownload } from "react-icons/fa6";

const IconComponent = () => {
  const iconStyle = {
    color: "black",
    fontSize: "50px",
    margin: "20px",
  };

  return <FaDownload style={iconStyle} />;
};

export default IconComponent;
